spring:
  application:
    name: blog-api

  datasource:
    url: jdbc:postgresql://ep-fancy-fog-and51rpk-pooler.c-6.us-east-1.aws.neon.tech/blogdb?sslmode=require
    username: root
    password: npg_5tPgLc6FTdyv
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      idle-timeout: 10000
      max-lifetime: 30000
      connection-timeout: 20000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  jackson:
    serialization:
      write-dates-as-timestamps: false

server:
  port: 8080

cors:
  allowed-origins: http://localhost:4000,https://www.mdfaizahmed.in
